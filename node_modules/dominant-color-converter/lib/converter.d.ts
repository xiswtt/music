import { Palette } from './types';
declare class Converter {
    customColorList?: `#${string}`[] | undefined;
    constructor(customColorList?: `#${string}`[] | undefined);
    private _getOriginalPalette;
    private _compareColors;
    convert(url: string): Promise<Palette>;
}
export default Converter;
